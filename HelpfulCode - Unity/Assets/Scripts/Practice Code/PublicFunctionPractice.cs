using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PublicFunctionPractice : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
	/*
	// recieve 3 ints and return their sum
	public int Return3Integers (int num1, int num2, int num3) {
		return num1 + num2 + num3;
	}
	// recieve 3 floats and return them as vector3
	public Vector3 ReturnVector3 (float one, float two, float three) {
		return new Vector3 (one, two, three);
	}
	// recieve a string and float, if string is empty retun float, otherwise return 0
	public float ReturnIfEmpty (string words, float empty) {
		// variables are lowercase
		if (words == "") {
			return empty;
		} else {
			return 0;
		}
	}
	// recieve a boolean and return the opposite of the boolean
	public bool ReturnOpposite (bool StartValue) {
		if (StartValue == true) {
			return !StartValue;
		}
	}
	*/
}
